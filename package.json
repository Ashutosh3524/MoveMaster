{
  "name": "project1",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "ui": "npm run dev --prefix ui",
    "server": "nodemon server/app.js",
    "both": "concurrently -n SERVER,UI -c green,cyan \"npm run server\" \"npm run ui\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "concurrency": "^0.1.4",
    "nodemon": "^3.1.10"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
